[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "mdl-dataset-sdk"
version = "0.1.0"
description = "dataset_sdk SDK"
requires-python = ">=3.11"
dependencies = ["mdl-platform-core"]
