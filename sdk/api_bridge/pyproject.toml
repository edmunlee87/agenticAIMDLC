[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "mdl-api-bridge"
version = "0.1.0"
description = "api_bridge SDK"
requires-python = ">=3.11"
dependencies = ["mdl-platform-core"]
