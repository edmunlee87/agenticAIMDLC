[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "mdl-feature-sdk"
version = "0.1.0"
description = "feature_sdk SDK"
requires-python = ">=3.11"
dependencies = ["mdl-platform-core"]
