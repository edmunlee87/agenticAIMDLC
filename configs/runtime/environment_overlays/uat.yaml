# Overlay stub -- extend with environment-specific overrides.
# Fields here are deep-merged over configs/runtime/environment.yaml.
environment: uat
feature_flags:
  enable_rag: true
  strict_policy_mode: true
  dry_run: false
storage:
  artifact_store_uri: "s3://mdlc-uat/artifacts"
  audit_store_uri: "s3://mdlc-uat/audit"
  event_store_uri: "s3://mdlc-uat/events"
  workflow_state_uri: "s3://mdlc-uat/workflow_state"
