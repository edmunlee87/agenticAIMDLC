[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "mdl-scorecardsdk"
version = "0.1.0"
description = "scorecardsdk SDK"
requires-python = ">=3.11"
dependencies = ["mdl-platform-core"]
