[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "mdl-widgetsdk"
version = "0.1.0"
description = "widgetsdk SDK"
requires-python = ">=3.11"
dependencies = ["mdl-platform-core"]
