[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "mdl-eadsdk"
version = "0.1.0"
description = "eadsdk SDK"
requires-python = ">=3.11"
dependencies = ["mdl-platform-core"]
