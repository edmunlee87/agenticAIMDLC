[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "mdl-knowledge-sdk"
version = "0.1.0"
description = "knowledge_sdk SDK"
requires-python = ">=3.11"
dependencies = ["mdl-platform-core"]
